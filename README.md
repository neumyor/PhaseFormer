# PhaseFormer
For ICLR'26 review
We implemented the code using the PyTorch Lightning framework, with the corresponding configurations written in the run_* files.

For example, to run ETTh1, simply execute:
```python
python run_etth1.py
```