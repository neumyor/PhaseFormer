# 时序预测模型迭代开发 Agent 规则

本文档用于指导 Agent 在本仓库中围绕 PhaseFormer 时序预测模型进行持续、自主、证据驱动的迭代开发。目标不是一次性给出模型建议，而是在已有代码、数据和实验条件上持续提出假设、低成本验证、分析失败样本，并逐步形成更好的模型方案。

核心原则：

* 先理解项目，再修改模型；
* 先建立基线，再评价改进；
* 每次改动都要对应明确假设；
* 每次实验都要留下可复现、可审计记录，并且必须进行bad case分析；
* 每次失败都要转化为下一轮可执行方向；
* 模型改进必须体现明确的创新性和先进性，不能把简单堆叠已有分支或调一个极端超参数包装成新方法；
* 用户要求和显式退出条件在整个迭代过程中持续优先。
* 允许按用户要求使用测试集调参，但必须保存完整选择轨迹、明确披露 test-set selection，且不得将相关结果描述为盲测或无偏泛化估计。

---

## 0. 实验与高误差分析 Skill

当用户给出明确模型设想，并要求实现该设想、运行实验以及分析高误差或显著退化样本时，必须调用项目级 `experiment-and-error-analysis` Skill：

- Codex：`$experiment-and-error-analysis`，入口为 `.agents/skills/experiment-and-error-analysis/`；
- Claude Code：`/experiment-and-error-analysis`，入口为 `.claude/skills/experiment-and-error-analysis/`。

该 Skill 负责实现候选机制、记录 baseline/candidate 搜索、计算样本级误差、程序化筛选案例并生成可审计的 Markdown/PDF 报告。仅讨论设想、仅分析用户已经提供的汇总结果、只做 smoke test，或用户明确要求不运行实验时，不触发该 Skill。

Skill 定义六类最低审计产物；本文件、`MANAGE_RULES.md` 或具体实验计划要求的 checkpoint、环境、命令、运行日志等补充证据仍须保留，不得为了缩减为六个文件而删除。

---

## 1. 核心工件
> 现状说明：`docs/archive/ITERATION_BRIEF.md` 与 `docs/archive/ITERATION_LOG.md` 记录了历史科研轮次，作为可追溯档案保留。当前分支的现行迭代计划与记录分别使用 `EXPERIMENT_SEARCH_PLAN.md`（任务网格/搜索/成本）与 `docs/agent-log.md`（追加式维护日志）；新的科研过程建议把关键结论写入 `docs/agent-log.md` 并在需要时引用归档日志。


迭代过程中只维护两个核心文档工件，避免文档膨胀；实验配置、日志、预测结果、bad case 等证据产物按统一目录组织，并在核心文档中引用。

### 1.1 `docs/archive/ITERATION_BRIEF.md`

记录本次迭代任务的稳定信息。除非项目结构、用户要求、退出条件或当前最佳方案变化，否则不要频繁修改。

应包含：

* 项目结构理解：数据集位置、模型代码位置、训练入口、评估入口、配置位置、结果输出位置、重要依赖和硬件约束。
* 用户要求：记录用户原始要求，并转化为可执行约束，例如目标、方法边界、理论要求、实验要求、资源限制。
* 基线状态：当前默认模型、主要指标、训练/推理速度、显存/内存开销、已知问题。
* bad case review要求：对于bad case样本如何进行分析和归因
* 退出条件：用户显式退出条件优先；若用户未给出，则写入默认退出条件。
* 当前最佳方案：模型版本、实验编号、关键改动、主要指标、证据产物路径、仍存在的问题。

### 1.2 `docs/archive/ITERATION_LOG.md`

追加式记录每轮迭代，不覆盖历史。

每轮记录：

* 本轮目标：要验证什么，与用户要求或上一轮 bad case 的关系。
* 候选假设：名称、核心机制、理论直觉、预期收益、风险、最小验证方式。
* 假设选择理由：为什么优先做这个假设。
* 实验设置：实验编号、commit ID、数据集、模型版本、关键参数、命令、硬件环境、可比性说明。
* 实验结果：指标、训练速度、推理速度、资源开销、与基线/当前最佳的差异。
* bad case 分析：失败样本的定位、错误模式的分析、模型设计或训练方法等因素的归因、下一步的优化决策动作。
* 迭代决策：保留、修改、合并或淘汰哪些假设，当前最佳方案是否更新。

### 1.3 实验证据目录

核心文档只写结论和引用，实验产物统一放在：

```text
research_runs/<experiment_id>/
```

建议结构：

```text
research_runs/<experiment_id>/
  config.yaml              # 本次实验关键配置或参数快照
  commands.sh              # 实际运行命令
  metrics.csv              # 汇总指标
  runtime.md               # 时间、显存、硬件、依赖等运行信息
  predictions/             # 可选：预测值、真实值、样本索引
  bad_cases/               # 可选：bad case 表格、图或摘要
  notes.md                 # 可选：补充观察
```

不是每次实验都必须生成全部文件；但任何被用于关键结论、当前最佳方案或 bad case 归因的证据，都必须能从 `docs/archive/ITERATION_LOG.md` 追溯到对应产物。

---

## 2. 初始化流程

在开始任何模型修改前，必须完成初始化。

### 2.1 阅读项目结构

先阅读项目目录和关键文件，明确：

* 数据如何加载；
* 模型在哪里定义；
* 训练流程如何启动；
* 测试指标如何计算；
* 结果如何保存；
* 是否存在多个数据集、多个任务或多个模型入口；
* 是否有 README、配置文件、脚本或已有实验记录。

本仓库初始阅读重点：

* `README.md`
* `src/dataset/data_info.py`
* `src/dataset/data_factory.py`
* `src/dataset/data_loader.py`
* `src/models/PhaseFormer.py`
* `src/utils/metrics.py`
* `config/base_config.py`
* `run_*.py`
* `run_all_experiments.py`

禁止在尚未理解项目结构前直接修改模型。

### 2.2 建立初始基线

基线必须分层建立，避免一开始就跑全量实验。

正式提升验证的固定参照是 `docs/PHASEFORMER_GOLD_STANDARD.md`。重新训练的
original baseline 属于 matched rerun，用于确认代码、环境和评估协议的偏差，不能
替换金标准。只有实验设置与金标准一致时，才能直接声明相对金标准的提升；否则应
明确标记为实验内配对结果。

推荐顺序：

1. **静态检查**：确认依赖、入口脚本、数据路径、GPU/CPU 可用性。
2. **smoke test**：用最小成本确认训练和评估链路可跑通。
3. **快速基线**：使用小 epoch、少量 horizon、代表性数据集或子集，了解指标量级、速度和资源开销。
4. **重点基线**：只在用户目标相关的数据集、horizon 或场景上扩大实验。
5. **全量基线**：仅当需要正式比较、资源允许、且快速基线已证明链路稳定时运行。

如果使用小训练轮数、数据子集、已有 checkpoint 或非完整评估，必须在 `docs/archive/ITERATION_LOG.md` 中标注“不可作为最终效果结论”。

### 2.3 固化用户要求

进入迭代前，把用户要求写入 `docs/archive/ITERATION_BRIEF.md`。用户要求不仅要记录原文，还要转化为 Agent 后续可执行的约束。

例如：

* 目标：提升模型对弱周期性时序数据的建模能力；
* 方法约束：需要引入明确新机制，而不是只调参；
* 理论约束：解释该机制为什么适合弱周期性；
* 实验约束：与当前基线和当前最佳方案比较；
* 验证要求：包含消融、重复 seed、多个 horizon 或 bad case 证据中的必要组合；
* 资源约束：优先低成本验证，只有证据充分时扩大实验。

---

## 3. 成本控制规则

Agent 应默认采用最低成本路径验证假设，只有在证据支持继续推进时才扩大实验规模。

* 不要默认跑全量数据集、全量 horizon 或长时间训练。
* 每轮先设计最小可验证实验，明确为什么这个实验足以支持或否定当前假设。
* 优先复用已有结果、checkpoint、日志和中间产物，但必须说明可比性。
* 新机制先做单数据集/单 horizon/短训练验证；只有出现正向信号或关键失败证据时，才扩展到更多数据集、horizon 或 seed。
* 长实验前应确认：代码已通过 smoke test，实验设置可复现，输出路径不会覆盖已有结果。
* 训练成本、推理成本和显存/内存开销是模型质量的一部分，不能只看预测指标。

---

## 4. 假设生成与选择

进入迭代后，生成若干候选假设。假设可以代表不同路线，例如周期建模、多尺度分解、频域增强、外生变量融合、损失函数重构或结构正则化。

每个假设应包含：

* 假设名称；
* 解决的问题；
* 核心机制；
* 理论直觉；
* 预期收益；
* 可能副作用；
* 最小验证实验。

候选假设还必须通过创新性筛选：

* 必须提出清晰的新建模机制、结构约束、归纳偏置、理论推导或与 bad case 模式直接对应的算法改进。
* 不能仅通过“新增一个 residual 分支”“把 residual gate 初始化为 0.999 或接近 1”“切换 loss/learning rate/batch size”等方式声称模型创新；这些最多只能作为消融、诊断或工程 baseline。
* 如果使用 residual、gate、ensemble、dataset-aware policy 等常见工程手段，必须说明它们相对于已有方法的新意在哪里，例如新的门控变量、新的相位可靠性估计、新的频域/相位空间约束，或由 bad case review 推导出的机制性设计。
* 不能过分依赖 dataset-aware 或 horizon-aware guardrail 来制造“最佳结果”。作为科学研究，首要目标应是提出可泛化、可解释的统一机制；按数据集或按 horizon 切换不同方案有明显投机取巧风险。
* 如果确实认为某些数据集或 horizon 必须采用不同机制，必须提供充分理由和证据，包括：数据性质差异的定量分析、bad case 模式差异、跨 seed 或跨 horizon 复核、消融对照，以及为什么统一机制无法覆盖这些差异。
* 不得在同一数据集的不同 horizon 上随意采用不同方案，除非能够证明 horizon 改变导致了明确的时序生成机制或错误模式变化。否则应优先使用同一机制和少量共享超参数。
* 对于论文式或正式方案，必须能说服审稿人该机制不是简单调参或后验选择，而是对弱周期时序数据性质的可泛化建模改进。

选择假设时优先考虑：

1. 与用户目标高度契合；
2. 机制简单，改动局部，可归因；
3. 有清晰理论直觉或时序建模动机；
4. 验证成本低，能快速得到有效信号；
5. 不破坏数据划分、评估协议和既有可比性；
6. 对当前 bad case 或已知瓶颈有直接解释力；
7. 尽可能避免使用dataset-aware的优化方案，除非你能够说服审稿人，不同数据集之间存在明显差异，必须采用不同的方案。
8. 尽可能避免 horizon-aware 的优化方案；同一数据集内不同 horizon 使用不同机制必须有明确机制解释和实验证据，不能只是因为某个 horizon 的指标更好。

不应优先选择复杂、昂贵、难以归因、主要依赖大规模调参的方案，除非已有证据表明简单方案不足。

---

## 5. 每轮迭代流程

每轮按以下顺序执行：

1. 从候选假设中选择一个优先验证；
2. 做最小必要实现，避免一次性引入多个无法归因的改动；
3. 运行与基线或当前最佳方案可比较的低成本实验；
4. 记录结果和证据产物路径；
5. 分析 bad case；
6. 判断假设应保留、修改、合并或淘汰；
7. 更新 `docs/archive/ITERATION_LOG.md`；
8. 如当前最佳方案变化，更新 `docs/archive/ITERATION_BRIEF.md`。

每轮必须回答：

* 这次改动想验证什么？
* 证据是否支持这个假设？
* 下一轮应该基于什么证据继续？

---

## 6. bad case 可审计规则

每次关键实验后都要寻找代表性 bad case。bad case 不应只是误差最大的样本，应尽量覆盖不同失败模式，但是总数应不超过10个，避免迭代太慢。

用于结论的 bad case 必须可审计，至少记录：

* 实验编号和模型 commit ID；
* 数据集名称、数据文件或 split；
* 样本索引、变量名或变量列；
* 输入窗口和预测窗口的时间戳或可替代定位信息；
* 预测值、真实值、误差度量；
* 对应图表或数据文件路径；
* 错误模式和归因（重要！！！）；
* 下一轮可执行优化动作。

如果当前评估脚本无法导出这些信息，允许先实现轻量、非侵入式的预测结果导出；导出逻辑不得改变训练和评估协议。

bad case 分析至少关注：趋势、周期、突变、峰值、低谷、长期漂移、噪声异常、变量间关系、训练稳定性、容量不足或过拟合、损失函数与目标不匹配等。

---

## 7. 理论与实验约束

提出新机制时，必须说明：

* 它解决什么问题；
* 原模型为什么可能不足；
* 新机制的理论直觉或建模动机；
* 新机制的创新性在哪里，为什么不是已有模块的简单拼接、残差捷径或极端 gate 设置；
* 如果方案包含 dataset-aware 或 horizon-aware 路由，必须单独论证其必要性、非投机性和可泛化性，并说明为什么统一模型不足；
* 可能副作用；
* 如何通过实验验证。

如果用户要求理论分析，不能只给工程解释。理论分析可以是直觉性、机制性或简化形式，但必须与模型结构和时序预测问题直接相关。

关键效果结论至少需要重复 seed 或多个 horizon 支撑；更强结论应进一步结合多个数据集、消融实验或 bad case 证据。

---

## 8. 实验记录规则

所有关键实验必须可复现。记录至少包括：

* 实验编号；
* 日期或轮次；
* commit ID 或主要代码改动；
* 数据集、horizon、split；
* 训练/评估命令；
* 关键参数；
* 主要指标；
* 时间和硬件开销；
* 证据产物路径；
* 结论和失败原因。

失败实验也要记录，例如代码错误、显存不足、训练不收敛、指标异常、数据读取问题或结果不可比。失败实验可以提供下一轮有价值的信息，不应直接丢弃。

允许依据测试集结果继续选择或修改配置。发生这种情况时，必须记录所有参与选择的配置、测试指标、修改顺序和最终选择依据，并在 `run.yaml`、迭代日志及最终报告中明确写明最终配置经过 test-set selection。测试集调参结果可以用于当前工程决策，但不能被表述为独立盲测证据。

---

## 9. 退出条件

用户显式退出条件永远优先。若用户没有明确退出条件，默认只有同时满足以下三类判断时才停止：

* **效果**：当前最佳方案在用户关注的数据集、horizon 或场景上优于基线或达到合理平衡；
* **证据**：关键结论有重复 seed、多个 horizon、消融或 bad case 证据支撑，并可追溯到实验产物；
* **资源**：继续迭代的预期收益低于计算、时间或工程成本，或遇到明确资源/数据限制。

停止迭代时，需要总结：

* 最终采用的模型方案；
* 相比基线的改进；
* 有效和淘汰的假设；
* 关键 bad case 的变化；
* 仍然存在的风险和后续方向。

---

## 10. 行为边界

Agent 在迭代过程中应遵守：

* 不在没有实验依据的情况下宣称模型有效；
* 不为了提升指标破坏数据划分或评估协议；
* 不覆盖已有基线结果和实验产物；
* 不一次性引入过多无法归因的改动；
* 不忽略 `docs/archive/ITERATION_BRIEF.md` 中的用户要求和退出条件；
* 不只根据平均指标判断模型优劣，应结合 bad case 和资源开销；
* 不把单纯调参包装成机制创新。

---

## 11. 总体工作节奏

1. 阅读项目结构；
2. 建立分层基线；
3. 写入用户要求与退出条件；
4. 生成候选假设；
5. 按优先级选择低成本假设；
6. 最小实现并运行实验；
7. 分析结果与 bad case；
8. 更新日志、证据引用和当前最佳方案；
9. 演化假设；
10. 重复迭代，直到满足退出条件。

最终目标是形成一个由实验、错误分析和用户约束共同驱动的自进化时序预测模型开发过程。
