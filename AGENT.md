# Agent Maintenance Guide

本文档描述负责通过 vibe coding 维护本仓库的 agent 行为规范。目标是在快速迭代的同时，保持改动可追踪、环境可复现、实验结果可核查。

## 基本原则

- 先理解再修改：开始任务前阅读相关代码、`README.md`、已有脚本和最近的变更记录，避免凭印象重写已有逻辑。
- 小步提交：一次只处理一个明确目标，避免把功能、重构、格式化和实验结果混在同一次改动中。
- 尊重现状：不随意替换框架、目录结构、训练入口或数据约定；如需调整，应在文档中说明原因和影响。
- 可复现优先：任何影响训练、评估、依赖、数据路径、随机性或指标口径的修改，都必须记录复现方式。
- 金标准优先：所有模型提升声明必须使用 `docs/PHASEFORMER_GOLD_STANDARD.md` 中相同任务的原始 PhaseFormer 结果；matched rerun 仅用于协议诊断，不得静默替换金标准。
- 不覆盖用户工作：发现未提交改动时，先确认其来源和影响；不得擅自回滚、删除或重置他人改动。
- 当用户提出自主科研、持续迭代模型、自动寻找改进方案、实验驱动优化等请求时，必须先参考 `HOW_TO_DO_RESEARCH.md`，并按其中的 `ITERATION_BRIEF.md` / `ITERATION_LOG.md` 机制（已归档于 `docs/archive/`）以及现行 `EXPERIMENT_SEARCH_PLAN.md` 组织工作。

## Git 管理

- 开始前执行 `git status --short`，确认工作区状态。
- 不使用破坏性命令，例如 `git reset --hard`、`git checkout -- <file>`、强制删除文件，除非用户明确要求。
- 每次修改模型、训练方式、训练脚本、文档等，均需要进行进行commit。
- 保持提交粒度清晰：
  - 代码修复、实验配置、文档更新尽量分开提交。
  - 提交信息使用简洁的祈使句，例如 `Fix PhaseFormer metric logging`。
  - 如改动影响实验结果，在提交信息或文档记录中说明验证命令和结果位置。
- 提交前检查差异：
  - 使用 `git diff` 审阅代码改动。
  - 使用 `git diff --stat` 确认改动范围没有异常扩散。
- 不提交大体积数据集、模型权重、临时日志、缓存目录或本地环境文件，除非仓库明确要求。

## 运行环境管理

- **优先使用可用的 conda 环境**，而不是 `uv`。本机已就绪的 conda 环境（含 torch 2.8.0 + PyTorch Lightning 2.1.0 及本仓库全部依赖）：
  - `py310`（`/home/niuyiming/.conda/envs/py310`）——首选，与仓库锁定的 Lightning 2.1.x 匹配，验证测试与本次修复均使用此环境。
  - `gift`、`basicts`——依赖齐全，可作为备用。
  - 运行命令使用完整解释器路径，避免依赖 `conda run` 或激活 shell：
    ```bash
    /home/niuyiming/.conda/envs/py310/bin/python <script.py>
    /home/niuyiming/.conda/envs/py310/bin/python -m pytest tests/ -q
    ```
- 只有在 conda 环境不可用或缺少依赖时，才 **fallback 到 `uv`**：
  - 如果仓库已有 `pyproject.toml` 或 `uv.lock`：
    - 安装依赖使用 `uv sync`。
    - 运行脚本使用 `uv run python <script>.py` 或 `uv run <command>`。
    - 新增依赖使用 `uv add <package>`，并提交同步更新后的锁文件。
  - 注意：仓库 `.venv` 默认是空的，`uv run` 首次会从零安装、耗时很长；只有当 conda 环境无法满足需求时才走这条路。
- 如果仓库尚未启用 `uv`，且 conda 不可用：
  - 不为临时任务随意引入新的项目结构。
  - 只有在用户同意或任务需要可复现依赖管理时，才初始化 `pyproject.toml`/`uv.lock`。
  - 初始化后需要更新 `README.md` 或相关文档中的安装方式。
- 不把虚拟环境目录提交到仓库。常见目录如 `.venv/` 应保持未跟踪或写入 `.gitignore`。
- GPU、CUDA、PyTorch 版本等环境差异会影响结果；运行训练或评估时必须记录所使用环境（conda 环境名或 uv）及关键版本信息。

## 代码修改规范

- 遵循现有代码风格、命名方式和模块边界。
- 优先复用已有工具函数、数据加载逻辑、模型配置和训练脚本。
- 只在必要时增加抽象；不要为了风格统一进行大范围无关重构。
- 对复杂逻辑添加简短注释，解释动机或约束；避免重复代码表面含义。
- 修改训练、评估或指标逻辑时，必须明确说明是否改变历史结果的可比性。
- 涉及随机性的代码应检查 seed、数据划分和 deterministic 设置，确保实验可复现。

## 数据与实验规范

- 默认数据目录遵循 `README.md` 中的 `resources/all_datasets/` 结构。
- 不提交原始数据集、生成的大型中间文件、模型 checkpoint 或 TensorBoard/CSV 大量日志。
- 新增实验脚本时，应说明：
  - 数据集名称和路径。
  - 关键超参数，例如 `seq_len`、`pred_len`、`period_len`、batch size、学习率。
  - 运行命令。
  - 输出目录。
- 修改默认超参数时，应记录修改前后的值和原因。
- 训练结果汇总文件如果需要保留，应确保体积可控，并在文档中标注生成命令。

## 操作与改动记录

- 每次完成有实际影响的维护任务后，更新文档记录。推荐位置为 `docs/agent-log.md`；如果该文件不存在，应在首次需要时创建。
- 记录应简洁但足够复现，包含：
  - 日期和任务摘要。
  - 主要修改文件。
  - 关键命令，例如安装、测试、训练或评估命令。
  - 验证结果，例如测试通过、训练完成、指标文件路径，或未验证的原因。
  - 已知风险、后续事项或需要用户确认的问题。
- 对只读排查任务，也应在用户要求或结论影响后续维护时补充记录。
- 不把敏感信息写入文档，包括 token、私有路径、账号、远程地址凭据或机器专属秘密。

## 验证要求

- 能运行轻量验证时，不只停留在静态检查。
- 优先级建议（环境优先用 conda，见「运行环境管理」）：
  - 语法或导入检查：`/home/niuyiming/.conda/envs/py310/bin/python -m py_compile <file>`。
  - 单元测试或最小测试集：`/home/niuyiming/.conda/envs/py310/bin/python -m pytest tests/ -q`。
  - 训练脚本冒烟测试：在可接受时间内使用小配置验证主流程。
  - 完整训练：仅在用户要求、资源允许且数据齐备时执行。
- 如果无法验证，必须说明原因，例如缺少数据集、缺少 GPU、依赖未安装或运行时间过长。

## 沟通规范

- 面向用户汇报时，先说结果，再列出关键改动和验证情况。
- 对不确定事项保持明确：说明假设、风险和建议，不把猜测写成事实。
- 需要用户决策时，提供具体选项和影响，而不是开放式地要求用户自行判断。
- 发现仓库约定与用户请求冲突时，先说明冲突点，再给出最小可行调整方案。

## 禁止事项

- 不在未确认的情况下删除数据、日志、checkpoint 或用户未提交代码。
- 不把临时实验结果伪装成正式 benchmark。
- 不提交机器本地绝对路径、私有凭据或不可复现配置。
- 不在没有记录的情况下改变依赖版本、默认训练参数或指标计算方式。
- 不为完成单个任务引入重量级依赖或全局架构改造。
